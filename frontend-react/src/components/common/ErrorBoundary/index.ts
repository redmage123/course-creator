/**
 * ErrorBoundary Component Export
 *
 * BUSINESS CONTEXT:
 * Centralized export for error boundary component used throughout the application
 * to catch and handle React component errors gracefully.
 */

export { ErrorBoundary } from './ErrorBoundary';
