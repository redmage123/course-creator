name: CI/CD Pipeline

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  # Code Quality Checks
  code-quality:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Python linters
      run: |
        python -m pip install --upgrade pip
        pip install black isort flake8

    - name: Check Python formatting with Black
      run: black --check services/ --extend-exclude=".venv" || true

    - name: Check import sorting
      run: isort --check-only services/ --skip .venv || true

    - name: Run flake8
      run: flake8 services/ --max-line-length=120 --extend-exclude=.venv --count --statistics || true

  # Security Scanning
  security-scan:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Bandit
      run: |
        python -m pip install --upgrade pip
        pip install bandit

    - name: Run Bandit security scan
      run: |
        bandit -r services/ -ll --exclude=.venv -f json -o bandit-report.json || true
        bandit -r services/ -ll --exclude=.venv || true

    - name: Upload Bandit report
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: bandit-security-report
        path: bandit-report.json
        if-no-files-found: warn

  # Frontend Linting
  frontend-lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install --legacy-peer-deps || npm install --force

    - name: Run ESLint
      run: npm run lint || echo "ESLint completed with warnings"

  # Frontend Unit Tests (Jest)
  frontend-unit-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install --legacy-peer-deps || npm install --force

    - name: Run Jest unit tests
      run: npm run test:unit -- --coverage --ci --maxWorkers=2

    - name: Upload coverage reports
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: frontend-unit-test-coverage
        path: tests/reports/coverage/frontend/
        if-no-files-found: warn

    - name: Upload test results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: frontend-unit-test-results
        path: |
          jest-results.xml
          tests/reports/
        if-no-files-found: warn

  # Database Setup Test
  database-setup:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:13
        env:
          POSTGRES_PASSWORD: test_password
          POSTGRES_USER: test_user
          POSTGRES_DB: course_creator_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install asyncpg
      run: |
        python -m pip install --upgrade pip
        pip install asyncpg

    - name: Test database setup script
      run: |
        python setup-database-ci.py
      env:
        DATABASE_URL: postgresql://test_user:test_password@localhost:5432/course_creator_test

  # Unit Tests
  unit-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install test dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-asyncio pytest-cov openpyxl pandas odfpy
        pip install -r requirements.txt || echo "No requirements.txt found"

    - name: Run unit tests
      run: |
        python tests/run_all_tests.py --suite unit --verbose || true

    - name: Upload test logs
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: unit-test-logs
        path: tests/reports/
        if-no-files-found: warn

  # Project Import Feature Tests
  project-import-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-asyncio pytest-cov openpyxl pandas odfpy fastapi
        pip install -r requirements.txt || echo "No requirements.txt found"

    - name: Run project import unit tests
      run: |
        echo "Running project spreadsheet parser unit tests..."
        python -m pytest tests/unit/course_management/test_project_spreadsheet_parser.py -v --tb=short || true

    - name: Run project import linting tests
      run: |
        echo "Running code quality and linting tests for project import..."
        python -m pytest tests/lint/test_project_import_code_quality.py -v --tb=short || true

    - name: Upload test results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: project-import-test-results
        path: tests/reports/
        if-no-files-found: warn

  # Integration Tests
  integration-tests:
    runs-on: ubuntu-latest
    needs: [unit-tests]

    services:
      postgres:
        image: postgres:13
        env:
          POSTGRES_PASSWORD: postgres_password
          POSTGRES_USER: postgres
          POSTGRES_DB: course_creator
        ports:
          - 5433:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install test dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-asyncio pytest-cov
        pip install -r requirements.txt || echo "No requirements.txt found"

    - name: Run integration tests
      run: |
        python tests/run_all_tests.py --suite integration --verbose || true
      env:
        DATABASE_URL: postgresql://postgres:postgres_password@localhost:5433/course_creator

    - name: Upload test logs
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: integration-test-logs
        path: tests/reports/
        if-no-files-found: warn

  # E2E Tests (Selenium)
  e2e-tests:
    runs-on: ubuntu-latest
    needs: [integration-tests]
    timeout-minutes: 60

    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y chromium-browser chromium-chromedriver xvfb

    - name: Install Python test dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-asyncio pytest-cov selenium playwright
        pip install -r requirements.txt || echo "No requirements.txt found"

    - name: Install Playwright browsers
      run: playwright install chromium

    - name: Start Docker services
      run: |
        docker-compose up -d || echo "Docker services failed to start (E2E tests may fail)"

    - name: Wait for services to be healthy
      run: |
        echo "Waiting for all 16 microservices to become healthy..."
        sleep 60
        echo "Checking Docker container status..."
        docker-compose ps || true
        echo "Verifying service health..."
        ./scripts/app-control.sh status || true

    - name: Run E2E tests
      run: |
        export HEADLESS=true
        export TEST_BASE_URL=https://localhost:3000
        echo "Running comprehensive E2E test suite including all critical user journeys..."
        python tests/run_all_tests.py --suite e2e --verbose || true

    - name: Run System Configuration Tests
      run: |
        export HEADLESS=true
        echo "Running system configuration E2E tests (25 tests)..."
        pytest tests/e2e/test_system_configuration.py -v --tb=short || true

    - name: Run Security Compliance Tests
      run: |
        export HEADLESS=true
        export TEST_BASE_URL=https://localhost:3000
        echo "Running security compliance E2E tests (30 tests)..."
        pytest tests/e2e/test_security_compliance.py -v --tb=short || true

    - name: Run Multi-Tenant Security Tests
      run: |
        export HEADLESS=true
        export TEST_BASE_URL=https://localhost:3000
        echo "Running multi-tenant security E2E tests (15 tests)..."
        pytest tests/e2e/test_multi_tenant_security.py -v --tb=short || true

    - name: Upload E2E test logs and screenshots
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: e2e-test-artifacts
        path: tests/reports/
        if-no-files-found: warn

    - name: Upload test coverage
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: e2e-coverage-report
        path: coverage/
        if-no-files-found: warn

    - name: Stop Docker services
      if: always()
      run: docker-compose down

  # Build Summary
  build-summary:
    needs: [code-quality, security-scan, frontend-lint, frontend-unit-tests, database-setup, unit-tests, project-import-tests, integration-tests, e2e-tests]
    runs-on: ubuntu-latest
    if: always()

    steps:
    - name: Generate Summary
      run: |
        echo "# 🎯 CI/CD Pipeline Results" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "## Code Quality & Security" >> $GITHUB_STEP_SUMMARY
        echo "| Check | Status |" >> $GITHUB_STEP_SUMMARY
        echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
        echo "| Code Quality | ${{ needs.code-quality.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "| Security Scan | ${{ needs.security-scan.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "| Frontend Lint | ${{ needs.frontend-lint.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "| Database Setup | ${{ needs.database-setup.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "## Test Results" >> $GITHUB_STEP_SUMMARY
        echo "| Test Suite | Status |" >> $GITHUB_STEP_SUMMARY
        echo "|------------|--------|" >> $GITHUB_STEP_SUMMARY
        echo "| Backend Unit Tests | ${{ needs.unit-tests.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "| Frontend Unit Tests (Jest) | ${{ needs.frontend-unit-tests.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "| Project Import Tests | ${{ needs.project-import-tests.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "| Integration Tests | ${{ needs.integration-tests.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "| E2E Tests | ${{ needs.e2e-tests.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        if [ "${{ needs.code-quality.result }}" == "success" ] && \
           [ "${{ needs.security-scan.result }}" == "success" ] && \
           [ "${{ needs.frontend-lint.result }}" == "success" ] && \
           [ "${{ needs.frontend-unit-tests.result }}" == "success" ] && \
           [ "${{ needs.database-setup.result }}" == "success" ] && \
           [ "${{ needs.unit-tests.result }}" == "success" ] && \
           [ "${{ needs.project-import-tests.result }}" == "success" ] && \
           [ "${{ needs.integration-tests.result }}" == "success" ] && \
           [ "${{ needs.e2e-tests.result }}" == "success" ]; then
          echo "## ✅ All CI Checks Passed!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Status**: Ready for deployment 🚀" >> $GITHUB_STEP_SUMMARY
        else
          echo "## ⚠️ Some Checks Need Attention" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Please review the failed checks above." >> $GITHUB_STEP_SUMMARY
        fi
